---
tags:
  - Backend
  - Database
  - SQL
---
1. between은 ~ 이상 ~ 이하 의 의미를 가지고 있다.
2. select 시 date_format(a, "%Y-%m-%d") 등으로 포맷을 변경할 수 있다.
3. select 문의 적용 순서는 FROM (1) > WHERE (2) > GROUP BY (3) > HAVING (4) > SELECT (5) > ORDER BY (6)이다.